This directory contains simple benchmark programs and script
infrastructure for generating graphs.  Just run `make` and everything
will be generated, provided you can run CUDA programs and have Gnuplot
installed.